# Astrology Times — Render-ready Python backend requirements
# No private CDN dependency. Installs directly from PyPI.

fastapi==0.110.1
uvicorn[standard]==0.25.0
python-dotenv==1.0.1
starlette==0.37.2

# Database (MongoDB Atlas)
motor==3.3.1
pymongo==4.5.0

# Models / validation
pydantic==2.6.4
pydantic-core==2.16.3
email-validator==2.1.1

# Auth
PyJWT==2.8.0
passlib[bcrypt]==1.7.4
bcrypt==4.1.2
python-multipart==0.0.9

# Payments
razorpay==2.0.1
setuptools>=68.0.0  # razorpay imports pkg_resources from setuptools

# Networking
httpx==0.27.0
requests==2.31.0

# PDF invoices/reports
reportlab==4.1.0

# CORS already bundled with starlette
